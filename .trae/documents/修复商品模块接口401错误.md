## 问题分析

1. **JWT拦截器配置**：在`WebMvcConfig.java`中，JWT拦截器正确配置了需要拦截的商品相关接口
2. **拦截器实现**：`JwtInterceptor.java`的实现看起来是正确的，它会检查Authorization头、验证token有效性并解析用户ID
3. **Swagger配置问题**：`SwaggerConfig.java`中配置了JWT认证方案，但是存在配置问题
4. **用户控制器示例**：`UserController.java`中的`getInfo`方法正确地从request属性中获取用户ID

## 解决方案

1. **修复Swagger配置**：
   - 修改`apiKeyScheme()`方法，使用正确的方式配置JWT认证方案
   - 确保安全引用的名称与认证方案的名称匹配

2. **检查JWT拦截器**：
   - 确保拦截器正确处理token验证
   - 确保拦截器正确设置用户ID到request属性中

3. **测试验证**：
   - 使用Swagger UI测试需要authorization的商品接口
   - 确保接口能够正确获取用户ID并执行相应操作

## 实施步骤

1. 修改`SwaggerConfig.java`中的`apiKeyScheme()`方法，使用`springfox.documentation.service.ApiKey`来配置JWT认证方案
2. 修改`securityContext()`方法，确保只对需要认证的路径应用安全上下文
3. 重启应用
4. 使用Swagger UI测试商品模块的需要authorization的接口

## 预期结果

- 商品模块中需要authorization的接口能够正确接收和验证token
- 接口能够正确获取用户ID并执行相应操作
- 不再返回401错误

## 代码修改点

1. `SwaggerConfig.java`：修改JWT认证方案配置
2. 不需要修改JWT拦截器和商品控制器代码

现在我将实施这个解决方案。